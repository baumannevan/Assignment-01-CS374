to complie the code simply enter into the command line,
	$make
I have created a make file so this command should create an executable file name movies.
To run movies executable,
	$movies movies.csv
replace movies.csv with arbatary movies data csv assuming it follows the same format.
